#pragma once
#include <cstdlib>

#define BYTE unsigned char //!< Binding for unsigned char
#define BYTE_BITS 8 //!< Byte size in bits
#define SHORT_SIZE sizeof(short) //!< Short size in bytes
#define INT_SIZE sizeof(int) //!< Int size in bytes
#define LONG_SIZE sizeof(long) //!< Long size in bytes
#define BYTE_SIZE sizeof(BYTE) //!< Byte size in bytes (?)
